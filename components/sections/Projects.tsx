import SectionHeader from "@/components/ui/SectionHeader";
import { projects } from "@/data/projects";
export default function Projects(){return <section className="section" id="projects"><div className="container"><SectionHeader eyebrow="Selected work" title="Projects that combine software and delivery engineering." copy="These projects demonstrate cloud architecture, infrastructure automation, CI/CD, Linux administration, and backend application development."/><div className="projects-grid">{projects.map((p,i)=><article className="card project-card" key={p.title}><span className="project-number">{String(i+1).padStart(2,"0")}</span><h3>{p.title}</h3><p>{p.description}</p><div className="tags">{p.technologies.map(t=><span className="tag" key={t}>{t}</span>)}</div><div className="project-links"><a href={p.repository} target="_blank" rel="noreferrer">Repository ↗</a></div></article>)}</div></div></section>}
